{
  "name": "aegis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "contract",
    "backend",
    "frontend"
  ],
  "scripts": {
    "env:up": "docker compose up -d --wait",
    "env:down": "docker compose down"
  }
}
